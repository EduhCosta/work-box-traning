import styled from 'styled-components';

const SectionName = styled.p`
    font-size: 18px;
    text-transform: uppercase;
`;

const CountPizzaBox = styled.div`
    position: relative;
    padding: 12px;
    border-bottom: 1px solid #CCC;
    margin-bottom: 12px;
`;

export { SectionName, CountPizzaBox };
